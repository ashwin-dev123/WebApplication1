@using WebApplication1.ViewModels
@model List<StudentViewModel>


<h2>Student List</h2>

<table>
    <thead>
        <tr>
            <th>Name</th>
            <th>Age</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var student in Model)
        {
            <tr>
                @*  here @ is razor syntax to help write c# code in HTML code *@
                <td>@student.Name</td>
                <td>@student.Age</td>
            </tr>
        }
    </tbody>
</table>
